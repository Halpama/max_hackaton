# Feature modules

Сюда кладём бизнес-фичи (FSD-lite).

Пример:

```text
features/profile/
  ui/
  model/
  api/
  index.ts
```

Страницы (`pages/`) собирают фичи. В `shared/` — только переиспользуемое без доменной логики.
